# Policy Pulse – Docs

- **Setup**: See root [README](../README.md) for environment variables and deployment.
- **Facebook CAPI**: Implementation notes live in the repo (netlify/functions and assets/js).
- **Postback**: MaxBounty callback URL format is documented in `netlify/functions/postback.js` (comment block).

No secrets or keys are stored in the repo; all sensitive values come from environment variables.
